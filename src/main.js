import React from "react";
import ReactDOM from "react-dom";

import "./style.scss";
import "./components/header";

const root = document.getElementById('root');
const app = (<h1>now with the JSX</h1>);

ReactDOM.render(app, root);


